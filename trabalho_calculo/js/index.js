function sumir(){
    /*
    var novoP = document.createElement("p")
    var novoTxt = document.createTextNode("joão lixo")
    var tamanho = 0;
    tamanho++;

    novoP.appendChild(novoTxt)
    document.body.appendChild(novoP)

    novoP.clientWidth(tamanho)
    */

    var divPrincipal = document.getElementById("placa")
    divPrincipal.style.display = "none";
}